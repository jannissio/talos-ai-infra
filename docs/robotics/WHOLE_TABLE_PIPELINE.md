# Shared spatial action pipeline for the complete table

The [single authoritative goal](../PC_HANDOFF.md#current-goal---authoritative-after-every-continuation) governs this work. All seven loose items must be considered in jointly randomized tabletop scenes, with full yaw and explicitly addressed stable alternative orientations. This implementation plan replaces further disconnected preset optimization. The existing private dinner demonstration remains a fallback.

## Selected reference and compatibility

Use **CLIPort's parallel-gripper spatial pick/place methodology** as the shared policy reference: current calibrated visual observation and instruction produce a pick location/rotation and conditioned placement prediction. Its [official implementation](https://github.com/cliport/cliport) reports single-GPU operation using about 8.5–9.5 GB and exposes a PyTorch implementation. The [paper, Appendix D](https://arxiv.org/html/2109.12098v1) describes crop-based pick rotation for parallel grippers. Its standard policy is spatially planar; full 3D grasping and dynamic correction are limitations, not capabilities Talos inherits automatically.

This is an adapted reference, **not a trained Talos robotics policy yet**. The selected source at revision `2be5c47b5bb9bb7040ad90693288b87b1e18e7ad` is now imported under `third_party/talos_cliport`, with Apache/MIT notices and per-file upstream hashes. The published CLIP RN50 representation is downloaded and SHA256 verified; it is pretrained vision/language, not pretrained robot actions. The minimal additional dependencies install into the existing CUDA environment without changing Torch.

The first actual RTX 4070 smoke check passes for the pick-location and 36-rotation placement heads using a freshly rendered **256×320×6** whole-table RGB-D heightmap. One numerical optimizer step per head has finite gradients; peak Torch reservation is **5,899,288,576 bytes (5.49 GiB)**. Measured step times are 1.56/1.35 seconds in this smoke check, not sustained training throughput. It saves no trained checkpoint and supplies no manipulation result. The pick-angle/tilt/grasp adaptation and training on successful physical demonstrations remain open. Raw input, calibration, source hashes and the complete measurement are retained in `.run/spatial-runtime-v1`.

The upstream repository contains Apache-2.0 code, MIT-derived CLIP code and an explicitly GPL-3.0 U-Net helper. That GPL helper was neither downloaded nor copied. The adapted decoder uses original MIT `support.UpsampleMerge`; one frozen RN50 is shared across streams and remains in evaluation mode. Upstream implicit network/cache downloads are disabled. See [third-party notices](../../THIRD_PARTY_NOTICES.md); do not relabel the Apache source as MIT or call this an exact upstream reproduction.

RVT-2 offers a stronger native 3D reference, but its [official code and pretrained model terms](https://github.com/NVlabs/RVT#license), hardware setup and runtime integration add substantial migration work. It is not a parallel rewrite in this plan. No RVT/SmolVLA weights are integrated.

## One coherent implementation milestone

1. **Joint scene and observation interface.** All seven loose objects share the tabletop sampling domain. Sample full yaw and declared alternative orientation families, settle using ordinary physics, and record geometric proposal rejection, instability and changed resting orientation. No learned success box determines positions. Export calibrated camera observations suitable for spatial action prediction. If RGB-D is used, it is an explicit camera sensor input; exact body poses/segmentation remain demonstration/scoring data, not hidden policy observations. Previous RGB-only model claims remain unchanged.
2. **Physical demonstration and execution interface.** Search multiple grasp contacts, approach directions and arms using the robot model and physical pinching. Add side plate, glass and tabletop cutlery explicitly. Test transfers to intended place settings in the actual joint obstacle scene. Preserve planning failures as unsolved cases. Add temporary-clearance actions and task ordering where destinations/routes are blocked. Successful contact, supported lifting, release and final placement must be measured; hypothetical IK alone is insufficient. Exact-state teachers may generate demonstration labels, never be misreported as learned control.
3. **Shared visual pick/place policy.** Reuse the reference's spatial prediction structure and language features. Supervise from successful physical demonstrations over the shared scene distribution. Record gripper pose/orientation/closure keyframes and recovery states, rather than asking a time index to reproduce one scene's joint trajectory. The parallel-gripper method handles yaw; height/tilt and alternative grasps require explicitly evaluated adaptations, not an upright-only redefinition of the goal. Keep the useful bottle observer/local motor components where compatible.
4. **Observe–execute–verify–recover loop.** Predict from the current scene, execute a short segment, observe again and verify progress. Collision-aware robot-model planning is a distinct hybrid execution option; if used, label the controller accordingly instead of claiming every joint target is neural. Plans must use camera-derived obstacle estimates online. Preserve torque/contact safeguards and do not attach or teleport objects. Re-plan arm/order/grasp after a failed attempt within a declared recovery budget.
5. **Complete randomized table evaluation.** Evaluate all requested items together from fresh scene seeds, record all valid-start failures and intended final placements, and report each item's orientation/position coverage plus full-table completion. A sampler or isolated grasp success does not count as table-setting success. Keep the hard cases visible and preserve a fresh final set separate from training/selection.

These steps form one end-to-end deliverable. Do not spend the remaining work on unrelated media, repeated unchanged baseline tests or another succession of narrow model fits. If complete randomized table setting is unfinished at handover, state that plainly and keep the verified fallback accurately labeled.

## Shared implementation in progress

`simulation_lab/table_observation.py` renders three fixed calibrated RGB-D cameras and fuses visible surfaces into the metric tabletop grid. It reads no object identity, segmentation or body pose. Unknown cells remain explicit for future obstacle planning. An independent tilted-camera/known-box test exposed a 2.5 mm MSAA depth bias; the sensor framebuffer now disables MSAA without changing existing renderer settings. Optical-depth geometry, pixel-center convention and grid coordinates pass their checks.

`simulation_lab/table_teacher.py` and `scripts/develop_whole_table.py` implement one **privileged demonstration-generation interface** for all seven loose objects: multiple contact families, both-arm selection, destination feasibility before touch, collision-checked joint paths/RRT, physical pinch/lift/hold/orient/place/release, and candidate temporary placement/regrasp search. The recorder retains failed searches, full motor/state traces and fresh camera observations. Each action is independently replayed; only passing actions can be applied by motors to the continuously evolving main scene. Physics lookahead is explicitly a training teacher, not learned control or online recovery.

The [first compact development archive](evidence/shared-table-pipeline-development-v1/README.md) retains **15 completed runs, 118 physical attempts and 300,126 state frames**. All 118 independent motor replays reproduce exactly. The one successful primitive is the explicitly narrow bottle positive control, also replayed into the continuously evolving main scene. **Zero randomized full tables complete.** Repeated development seeds and positive controls are not a generalization score. The original RGB-D arrays remain local; the archive binds 483 calibrated observations by canonical array hashes and preserves their previews, recipes and every motor trace.

The first random scene supplies physically supported plate lifts/holds and some transfers, but final placement is still unsolved. Thin side-plate grasps slip or press into the compliant table geometry. Keeping a suitable tool axis and grasp yaw avoids unnecessary wrist turns; support-based lowering now allows bounded correction attempts without releasing an unsupported item. A separate natural-axis/placement refinement job, `.run/whole-table-development-v11-seed4001`, was still running when the archive was frozen and is explicitly excluded from its totals. Source snapshots and every failed attempt remain under `.run/whole-table-development-*`.

Current work must solve grasp/release geometry for the five-joint arms and table-supported regrasp routes, then supply actual successful varied demonstrations to the shared visual policy. Stable inverted starts remain required and unsolved; a grasp that cannot release in the intended orientation needs a different contact or regrasp, not an invalid-start label. No learned spatial policy, complete randomized workflow, online camera obstacle planning or physical failure recovery is established. The running demo and selected fallback models remain unchanged.

The [V11 follow-up](evidence/shared-table-pipeline-followup-v11.json) finished with 36 further unsuccessful physical attempts and exact motor replay. The [V12–V19 checkpoint](evidence/shared-table-followup-inventory-v3.json) retains another 298 attempts and 939,801 frames, with 25 passing primitives and 11 accepted continuous-scene actions. V17 places both plates in one joint scene through a side-plate action and two-arm plate relay. Five final settings remain unfinished. V18 accepts a mug clearance; V19 accepts a measured sideways-to-upright mug buffer. The subsequent [V20–V22 report](evidence/shared-table-followup-v20-v22.json) adds 78 physical attempts/300,908 frames and retains two planning-only bottle diagnoses. V20 accepts one mug clearance; none of these runs completes a randomized table.

The shared teacher now uses bounded MuJoCo least squares for held-object orientation, with placement tolerances independent of the natural-axis parameterization. An unchanged six-skill calibration run confirms the 1 mm external-penetration guard is achievable; the guard remains unchanged. Reversed side-plate rim pinches and deeper glass finger-pad contacts have passing physical demonstrations. Glass final placement and stable inverted-object reorientation remain unsolved. An experimental scalar horizontal-axis constraint removes unnecessary bearing/axial rotation constraints for sideways glass and bottle; geometry tests pass, but this is not a passing sideways-placement capability.

Buffer selection retains exact motor continuity and checks for repeated arrangements. Besides a geometric final-transfer plan or obstruction removal, it can accept a measured improvement of more than 30 degrees toward the item's final upward normal, limited to two orientation-progress actions per item. Any newly blocked destination must be blocked by the moved item itself. This lets a physically righted mug stay in the evolving scene while final transfer is attempted; it does not certify the next action or table completion. Failed wrist configurations are excluded individually, with equivalent converged source poses deduplicated.

The unchanged glass setting has [checked goal contacts](evidence/glass-goal-geometry-v1.json), so its current failure cannot be labeled an impossible task target. Tool-local orientation constraints and a collision-checked 20 mm approach option enable additional pose solutions. Vessel placement tries preserved yaw before a free-yaw fallback; cutlery still has its required final yaw. None of these geometric changes guarantees retention. [Physical follow-through](evidence/glass-grip-loss-diagnosis-v1.json) identifies moving-jaw contact at the glass lip and increasing tilt/creep. A deeper contact passes lift/hold but still falls; it remains an unsuccessful demonstration. The [airborne](evidence/table-regrasp-diagnostic-v1.json) and [front-edge](evidence/table-edge-regrasp-diagnostic-v1.json) regrasp alternatives also remain unsolved, with geometry, reset-only support and actual manipulation reported separately.

`simulation_lab/spatial_demonstrations.py` indexes verified completed episodes, separating calibrated image/instruction inputs from privileged training labels and robot feedback. Pick labels denote a surface contact, not a gripper origin; actual gripper keyframes and declared/final item orientation are separate. Independent passing lookaheads can be included only with explicit origin and temporary-action instructions. Source-dependent samples are grouped and exact duplicate traces excluded. The current limited inventory is not sufficient evidence for a trained all-item policy; no Talos spatial fit or checkpoint exists. Current checks pass 85 application tests and 10 targeted pretrained-reference/adapter tests.

## Current evidence and missing behaviors

The joint randomizer's geometry tests pass. Its frozen scene experiment produces stable full arrangements in **14/16 development** and **63/64 final** scenes. The three invalid scenes remain; these are not manipulation trials. Every item spans the shared tabletop and all declared orientation families are represented. A conservative robot-chain bound excludes points outside its outer limit; it does **not** certify a feasible grasp or collision-free path.

The prior wider-bottle application comparison finishes **10/10 complete dinners versus 0/10** for the older controller from paired starts. Other items in those trials still use narrow starting distributions. All 36 app outcomes, 99,579 state frames and 8,996 bottle RGB observations are packaged. This is a useful reusable component, not the clarified whole-table goal. No new browser/cloud promotion is needed before the shared pipeline work.

| Item or fixture | Useful existing component | Required broader behavior |
| --- | --- | --- |
| Bottle | Live RGB approach/grasp, neural motor and two destination routes | Full workspace, yaw/sideways/inverted starts, obstacle-aware routing and recovery |
| Plate | Narrow learned placement and physical rim grasp | Workspace-wide rim selection, both faces, varied routes |
| Mug | Narrow placement with bounded late visual correction | Broad handle/body grasps and stable alternative orientations |
| Fork and spoon | Narrow learned drawer retrieval | Tabletop starts, either face, full yaw, arm/route choice |
| Side plate | Passing reverse-rim physical placement in exposed joint seed 4001 | Broader starts, inverted regrasp, learned control |
| Glass | Passing upright buffer placement from exposed joint seed 4004 | Final setting, alternative orientations, broader starts and learned control |
| Drawer | Narrow learned opening; configuration teacher diagnosis | Observed state, clearance, partial-opening/changed-configuration support and task integration |

The drawer clearance diagnostic recovers all **29** earlier plate-obstruction failures with unchanged contact control after an explicit reset-only plate relocation. Twelve grasp-pose planning failures remain. It establishes the cause of those failures, not autonomous obstruction clearing or a newly learned drawer policy.
