# Continue Talos on another Windows PC

## Current goal - authoritative after every continuation

**Develop and physically verify robust camera-driven manipulation across declared reachable object arrangements and relevant drawer configurations.** This is the user's September 14 priority correction. It supersedes conflicting historical next-step capsules below. Constrained preset success, repeated baseline checks and polished submission materials do not complete this broader goal.

On resuming after a new turn or context compaction, read this section first, then [the broader manipulation plan](robotics/BROADER_MANIPULATION_PLAN.md) and the current priority in [the active checklist](hackathon/FINAL_SUBMISSION_CHECKLIST.md). Continue substantive capability development; preserve the working submission package as the deadline fallback. Do not silently revert to media/release work as the primary goal.

**Current milestone:** the [100-case broader diagnostic](robotics/evidence/manipulation-coverage-v1/README.md) retains all 200 paired rollouts and 88,760 frames: learned 11/83 valid versus programmed 43/83, plus 11 invalid starts. [Bottle image refinement V1](robotics/experiments/bottle-refinement-v1.json) now stops at its fresh perception gate: 433/456 present (94.956%), 0/56 absent false accepts, 1.494 mm p95 / 4.427 mm maximum, against 95% / 0 / 2 mm / 4 mm. Both candidates, exact compact inputs and all outcomes are packaged and verified (242 manifest-bound files); its six development and 24 final physical seeds remain unexposed. No promotion.

**Completed physical diagnosis:** [exposed-grid V2](robotics/evidence/bottle-refinement-exposed-physics-v2/README.md) retains all 58 rollouts, 40,396 frames and 20,194 RGB observations. Live succeeds in 25/27 valid cases, frozen in 9/27, original learned in 3/27; two invalid resets also remain. The position grid alone improves from 0/24 planned to 21/24 (22 valid). Both remaining live failures concern neural motor support near [0.16,-0.06]. This is training-exposed diagnosis, not fresh generalization or promotion. The stopped V1 harness and all 663 bound evidence files are preserved.

**Active next stage:** [wider physical V1](robotics/experiments/bottle-wide-physical-v1.json) freezes the same observer, V5 motor and V2 R1 controller before eight fresh development scenes (live/baseline), then 32 fresh three-condition scenes and six exposed regressions only if its gate passes. Source X [-0.14,0.16], Y [-0.18,-0.06], yaw +/-0.6; requested destinations [0.10,-0.115] and [0.20,0.05]. The failed middle destination is explicitly unsupported. Fresh scene seeds also vary other-object starts, mass +/-8%, friction +/-10% and light 0.92..1.06 through the original scene generator. Development passes 8/8 live (4/4 per goal) versus 1/8 baseline; the unchanged candidate is ready for its frozen final stage. The earlier standalone perception gate remains failed and its reserved physical seeds remain untouched; this separate protocol evaluates complete task execution under the unchanged physical criteria. No new fit or candidate change is allowed. Resume the development gate, then the frozen final stage only on a pass, followed by guarded integration/production checks. Continue other-object/drawer development after establishing the bottle method; do not let media work replace that priority.

**Verified fallback:** six learned dinner skills, two bottle relay directions and bounded live correction during late mug placement. The live/baseline 20/20 comparison is narrow and does not establish broader coverage. The September 14 human voice command `Set the  table.` now completes all six skills in 247.655 simulated seconds, with both arms parked and no reported unexpected collisions; [the original result](robotics/evidence/human-voice-dinner-v1/result.json) is retained. The latest private cloud live-mug trial also passes, in 287.79 wall seconds. Actual legacy Intel CPU/iGPU inference and Intel UHD rendering are verified for the preceding baseline. Changed live-mug components have a new compact verification kit; qualifying Core Ultra Series 2/3 eligibility remains unresolved.

**Deadline and persistent constraints:** handover target September 14, 18:00 CEST; official deadline rechecked September 14 is September 16, 20:30 CEST. Keep GitHub and Hugging Face private until agreed final release timing. NEVER click final Submit; the user submits personally. Preserve all models/evidence and failed attempts, use neutral author/committer metadata, never commit credentials/private files, and check unused output plus log paths and at least 10 GiB reserve before every new dataset/export. No further microphone action is needed for the completed rehearsal.

## Historical status capsules

The following records are history, not the current work priority. Their pending items may have been resolved above.

**Current deployment, September 14:** the verified live-mug profile is installed as **Learned dinner · live mug vision**; use **Set the table** at `http://127.0.0.1:8770/`. Both production entry points pass standard and farther-left seed-42 workflows. The hosted-preview repair changes no recorded physics/control array; both attempts and 42,492 frames are [preserved](robotics/evidence/visual-mug-deployment-v1/README.md). Final application/training checks pass **73/20**. Private cloud deployment and the human six-skill rehearsal are next; the old 8768 service is still available. All preceding capsules below are historical and must be read with this current status.

**Intel baseline now verified:** [four laptop runs](robotics/evidence/intel-final-legacy-v3/README.md) preserve all 169 supplied manifest entries and 19,820 state frames. The two later runs use **Intel UHD OpenGL rendering** plus Intel CPU or GPU.0 inference; both complete all six skills on exposed seed 42. Earlier NVIDIA-rendered runs and the failed generic-GPU attempt remain preserved, as do two stopped evidence-auditor attempts. All four match the original six source checkpoints. This completes actual Intel execution/rendering of the preceding baseline. The i7-10850H is not Core Ultra Series 2/3, so strict eligibility remains false. The newer live-mug option still needs a separately scoped laptop run. Never click final Submit; GitHub/HF remain private.

**September 14 final mug comparison:** [V2](robotics/evidence/mug-visual-correction-v2/README.md) passes both frozen gates with unchanged controller and weights. Development: baseline/live 6/6 per preset, frozen 0/6. Final: baseline/live 10/10 per preset, frozen 0/10. All 96 workflows, 422,745 frames and 824 correction queries are retained; every neural correction reproduces exactly. All 653 core package files and 96 portable scenes pass independent verification. This demonstrates bounded late-mug live feedback relative to the frozen-image control; it does not improve success over the original baseline on these scenes. A separate guarded browser/hosted integration is prepared and tested, but has not yet been promoted or deployed. Incoming legacy Intel laptop evidence is locally preserved and being audited. Never click final Submit.

**September 14 physical mug integration:** [V1](robotics/evidence/mug-visual-correction-v1/README.md) retains all 36 workflows and 63,734 frames. Baseline passes 6/6 per preset; both experimental modes stop at the first camera observation, before any correction. A no-step saved-state diagnosis identifies renderer replacement order: closing the old renderer first restores all three original images and the initial neural output exactly. The V1 gate stays failed, all 298 core evidence files pass package-only verification, and its final scenes stay unexposed. A [separate V2 protocol](robotics/experiments/mug-visual-correction-v2.json) freezes that setup repair with identical controller/weights/settings and new development/final seeds; its 36 development workflows are in progress. No selected or hosted controller change yet. Never click final Submit.

**Deadline clarified September 14:** the organizer's event page explicitly states **September 16, 20:30 CEST**, and the schedule agrees. Retain the user's **September 14, 18:00 CEST handover target**. The online track still explicitly names Core Ultra Series 2/3; a legacy i7 laptop run cannot establish that hardware requirement. The signed-in submission draft's application step still contains the saved repository/demo links and 1,962-character text; Submit remains untouched.

**Latest motor preparation:** [joint-limited motor V2](robotics/evidence/mug-correction-motor-v2/README.md) retains all 9,216 prior configurations and passes every revised input label, one 6,000-update fit, CPU export and all 1,024 fresh kinematic cases (0.061 mm p95 / 0.131 mm maximum). The callable package reproduces every output exactly. Errors refer to scaled local steps, not guaranteed 2 mm movement or physical success. Both candidates, all inputs/results and two pre-data launcher incidents are preserved. Next: separately declare and run live-versus-frozen-image physical mug correction using the passing stereo observer. No selected dinner or hosted model changes; GitHub/HF stay private and final Submit remains the user's action.

**Active goal (September 13):** finish the broader hackathon system and submission by September 14. Follow [the final-submission checklist](hackathon/FINAL_SUBMISSION_CHECKLIST.md), which supersedes the narrow release scope below. User has confirmed a successful microphone command on this PC; credentials remain local.

**Latest stereo mug observer:** the separately declared [image-point observer](robotics/evidence/mug-keypoint-observer-v1/README.md) now passes development, FP32 export, all 512 fresh synthetic cases (448/448 present, 0/64 absent, 0.270 mm p95 / 0.422 mm maximum) and all 144 exposed physical states (0.617 / 0.893 mm). A 173.022-second RTX 4070 fit uses 2.914 GiB peak Torch reserve. Both checkpoints, exact compact inputs, original outcomes and the stopped audit attempt are preserved. Packaged reproduction regenerates all 1,024 fresh RGB views and every selected prediction exactly without raw files. This verifies bounded perception only; a separate live-versus-frozen corrective motor experiment is next. Selected dinner models stay unchanged, GitHub/HF remain private, and final Submit remains exclusively the user's action.

**Motor follow-through:** the separately declared [local differential motor input gate](robotics/evidence/mug-correction-motor-v1/README.md) stops before fitting. Fixed ≤2 mm translations accept only 6,853/8,192 training and 857/1,024 development points against 99%; every rejection exceeds the 0.08 rad joint-step cap. Separate forward geometry and finite-difference audits verify the labels. All 9,216 attempts and sources are packaged; no model/export/physics follows, and seed 2026100103 remains unexposed. Next hypothesis: joint-limited correction sizing under a new protocol, then a separate physical comparison. Never relax or resume the stopped protocol.

**Compact laptop handoff:** the private [19.5 MB Intel verification kit](../submission/verification-v1/README.md) now includes the selected models, source/assets and licenses. A fresh isolated extraction passes all six physical skills and CPU export checks on this PC in 52.662 seconds, with the strict Intel flag correctly false. All reports and the state trace are retained. Use the kit with the laptop's existing Python environment; actual Intel execution/eligibility remains open. Full-repository handoff now budgets 2 GiB plus the reserve because the preserved archive has grown. The kit needs 1 GiB plus the reserve, excluding any dependency installation. Never submit automatically.

**Latest September 14 pose follow-through:** the separately declared [mug RGB pose fit](robotics/evidence/mug-visual-observer-v1/README.md) completes one 12,000-update run in 32.564 seconds / 0.082 GiB Torch-reserved memory. Both checkpoints accept all 448 present development cases and reject all 64 absent cases but fail accuracy: final 8.105 mm p95 / 23.172 mm maximum, against 1.5/3 mm limits. All 4,608 examples independently regenerate their 13,824 views and labels exactly; both failed models and every score are packaged. Stop this protocol before export/fresh seed 2026099803/physics. The next architectural hypothesis is semantic image-point prediction with calibrated triangulation, followed by separate physical correction tests. No selected model changes. Continue the full goal, keep GitHub/HF private, and never click final Submit.

**Latest camera diagnosis:** the separately declared [held-mug visibility test](robotics/evidence/mug-visual-visibility-v1/README.md) completes all 144 exposed placement states / 1,296 views. All three layouts have two usable views in every state; the focused 45°/225° layout wins the preregistered pixel-area tie break. RGB centroids still give 10.836 mm p95 body-midpoint error in that layout, so this is visibility evidence only. All images/masks are packaged and independently re-rendered with zero discrepancies. Next: a bounded RGB pose observer before attempting corrective motor control. No selected model or public-release status changes.

**Latest September 14 mug follow-through:** the separate [mug release fit](robotics/evidence/mug-release-v1/README.md) passes all 41 modified input replays and completes one 18,000-update fit/export, then fails its physical development gate. All 24 paired workflows finish: baseline 6/6 per preset, candidate 4/6 per preset. Four mug regressions are retained with 119,878 state frames; the candidate is not promoted and final seeds 2026099701–2026099712 remain unexposed. An [exposed-state diagnosis](robotics/evidence/dinner-mug-diagnostic-v1/README.md) is also preserved. Another similar warm fit is not justified; the next architectural direction is visual correction during placement, starting with an independently declared observability check. Continue the full goal, keep GitHub/HF private and never press Submit.

**September 14 spoon release follow-through:** the [bounded release experiment](robotics/evidence/spoon-release-v1/README.md) finishes 39/39 modified demonstration replays, one 12,000-update GPU fit and export parity. All 24 paired full-workflow development trials finish: baseline and candidate each pass 4/6 standard and 4/6 left-reach starts. Every reached spoon attempt passes, but two scenes fail at the unchanged mug step. The 5/6-per-preset gate fails; no promotion or final trial follows. All outcomes, 112,320 state frames, exact inputs and the candidate are preserved. The selected dinner suite and private hosted demo remain unchanged. Intel execution, the pending human six-skill voice rehearsal and final public release remain open; never click Submit.

**September 13 evening:** the packaged [v6 architecture](robotics/FINAL_ARCHITECTURE.md) now has six learned skills (8/10 frozen full sequences) and both learned bottle relay directions (5/5 each). Five dinner models, four relay models, exact compact inputs and every outcome are packaged and privately pushed on `codex/final-submission`. Updated media and all three submission-form steps are saved as a draft, verified after reload; **Submit has not been pressed**. The private Hugging Face Space now passes a fresh complete six-skill sequence on OpenVINO CPU: 247.655 simulated / 273.03 wall seconds on exposed seed 42. All 41 original protected files remain unchanged; current checks pass 56 application and 13 training-contract tests. Follow [active RGB-feedback work](robotics/RGB_SERVO_PROGRESS.md), not just the submission baseline. Final Intel execution, human six-skill voice rehearsal, final public release and actual submission remain open.

**Later September 13:** the separate RGB correction experiment completes all 48 frozen trials: 2/12 nominal live, 1/12 nominal frozen, 3/12 pushed live, 0/12 pushed frozen. Seven seeds are refused before movement. The observer passes its fresh synthetic gate, but physical coverage remains poor; the candidate is **not promoted**. [All outcomes and frozen sources](robotics/evidence/rgb-servo-v1/README.md), models and compact training reproduction inputs are packaged, with a labeled comparison video. Latest checks pass **61 application and 13 training tests**. Do not tune on those final seeds. The user has reiterated: continue preparation, but **never click the final submission button**; they will review and submit personally.

**Final September 13 routing result:** [V2](robotics/evidence/rgb-servo-v2/README.md) completes a new 48-case frozen set: 6/12 nominal live, 2/12 nominal frozen, 5/12 pushed live and 1/12 pushed frozen. It remains **unpromoted**; both development revisions, all failures and unchanged weights are retained. Current checks pass **67 application and 13 training tests**. The [final review packet](hackathon/FINAL_REVIEW_PACKET.md) identifies remaining Intel, human voice and public-access work. The portable six-skill verification procedure passes on this PC and correctly fails Intel eligibility; run it on the laptop with `verify-final-on-intel.ps1`. Keep GitHub and HF private, and leave Submit to the user.

**Later integration verification:** the real language/RGB planner completes a seven-step workflow from **Left reach practice**: two bottle relay legs followed by plate, mug, drawer, fork and spoon. All four development scenes pass; [8/10 new frozen full workflows](robotics/evidence/composed-dinner-v1/README.md) pass with unchanged models. Both failures are final spoon placement errors, and all traces plus an 82.9-second all-ten video are preserved. Use `set the table` with the learned dinner mode to reproduce. These final seeds are now exposed. This does not complete human voice rehearsal, Intel execution or broad placement coverage.

September 13 PC follow-through: see [RTX 4070 baseline verification](robotics/PC_BASELINE.md) for fresh installations, physical regression results and a successful human-triggered voice trial. The [coverage experiment](robotics/RTX4070_COVERAGE_EXPERIMENT.md) stopped at its declared data gate and retained the published model. Follow the [September 14 release plan](hackathon/RELEASE_PLAN_2026-09-14.md): the user requires completion tomorrow and gives September 15 as the deadline. Live timing, final Intel execution and judge-access details remain open. The original handoff below is retained as historical context.

**Late September 13 refinement:** [V3](robotics/evidence/rgb-servo-v3/README.md) uses a separate 6,000-step motor-training budget with geometry losses. It improves exposed kinematic scores but misses the unchanged 0.500 mm p95 gate (result 0.564 mm), so it stops without physical trials or promotion. Both checkpoints and every score are packaged; the reserved physical scenes remain unexposed. Current training checks pass **15 tests**; the unchanged application suite remains at **67 passed**. Continue from the final review packet, preserving GitHub/HF privacy and leaving Submit to the user.

**September 14 preparation:** the [evidence page](hackathon/PUBLIC_DEMO.md) now contains all five current videos and passes local asset/HTTP hash and browser playback-selection checks; preview at `http://127.0.0.1:8769/`. Pages remains disabled and manual-only. Both learned relay directions also pass fresh private hosted CPU checks on exposed seed 42, with all legs released and parked. All form steps, media and the 1,897-character additional-information text persist after reload. The entry is still a draft; no final submission action has been taken. Verify the latest reviewed private `main` and development revisions before release, especially after later Intel evidence is added.

Clone https://github.com/jannissio/talos-ai-infra and open the checkout as a Codex project. Current final-submission development is on `codex/final-submission`; use that branch until the reviewed final release is merged. The old laptop's absolute path is not required.

**September 14 motor/hosting follow-through:** [V4](robotics/evidence/rgb-servo-v4/README.md) stops offline at 0.512 mm p95. [V5](robotics/evidence/rgb-servo-v5/README.md) passes its offline gate at 0.367 mm, then stops at its physical development gate: 3/6 nominal live, 1/6 nominal frozen, 3/6 pushed live, 0/6 pushed frozen. All 24 cases and 13,711 state frames are packaged; final seeds stay unexposed and the selected dinner baseline is unchanged. The private hosted relay-plus-dinner workflow also passes all seven steps on exposed seed 42 in 297.31 wall seconds. Its hosting allowance is now 420 seconds, with local cancellation/restart verified; check the latest private deployment record for the subsequent cloud regression. Continue preparation with the active checklist, preserving privacy and never clicking Submit.

## Read first

- `README.md`
- `docs/robotics/SUBMISSION_VERIFICATION.md`
- `docs/robotics/VISUAL_BOTTLE_POLICY.md`
- `models/bottle_visual/README.md`
- `training/bottle_visual/README.md`
- `docs/robotics/TABLE_RELAY.md`
- `docs/robotics/STORAGE_POLICY.md`
- `docs/hackathon/LIVESTREAM_REVIEW_2026-09-11.md`
- `submission/PROJECT.md`

## What transfers through Git

The repository includes simulation code and robot/dinner assets, both deployed bottle models, the exact compact 22-episode upright training input, training/evaluation scripts, published metrics, the presentation, cover and two HD videos. This is enough to run the packaged demonstration and retrain the compact upright model.

Excluded on purpose: `.env`, virtual environments, `.run` with raw images/recordings and experimental checkpoints, private source downloads/transcript and machine diagnostics. The complete experiment archive remains on the laptop. Do not assume those private paths exist on the PC. New demonstrations can be generated with the collectors; copying the old raw archive is optional for further analysis and should be selective after checking disk space. Never upload the raw archive or credentials indiscriminately.

Create a new local `.env` on the PC if voice is needed: `SPEECHMATICS_API_KEY=your-key`. Matching quotes and spaces around `=` are supported. Obtain the key privately from the account or transfer the local file securely; do not paste it into chat or commit it. No account credit purchase is authorized.

## First goal on the PC

Establish a working baseline on the RTX 4070 (12 GB VRAM) before changing models:

1. Inspect installed clean CPython 3.12, GPU/driver information and actual free disk space. Create new `.venv` and `.venv-training` environments using the pinned requirements in the README. Do not copy Windows environments from the laptop or use Anaconda. Verify CUDA availability in the training environment; avoid an accidental CPU-only Torch installation.
2. Run the documented application and training-contract tests. Physically evaluate `models/bottle_visual` on seed 42 before retraining. These are exposed regression cases, not new holdouts. Preserve the packaged models unchanged.
3. Start `start-lab.ps1 -Learned`, verify the browser cameras and controls, and let the user rehearse actual microphone input. Speechmatics synthetic-audio API tests passed on the laptop; unattended human microphone capture was not tested.
4. Report any environment differences. OpenVINO CPU/iGPU measurements are specific to the laptop; do not reuse them as measurements of this PC. Core Ultra hardware eligibility still needs confirmation against the written brief and livestream clarification.

Then propose a bounded broader-position learning goal: retain the current baseline, inspect the failed wider-position cases/protocols, collect a compact curriculum rather than a large image archive, train on the RTX 4070, and declare fresh evaluation seeds before checkpoint selection. More GPU capacity alone does not solve grasp precision or dataset coverage. Consider continuous visual feedback as a subsequent architectural improvement, not an already implemented feature.

## Current measured scope

- Programmed exact-state physical skills place bottle, plate and mug, open the passive drawer, and retrieve fork/spoon. The six-skill sequence passed on seed 42.
- The programmed left-to-right bottle relay releases onto a shared table area before regrasping with the other arm. It is not an airborne handoff; reverse direction and other objects are not supported by this relay.
- The default upright model uses a **classical overhead RGB centroid detector**, a neural trajectory and motor-feedback progress checks. It passed 10/10 declared new task-preset scenes, with small bottle-position jitter, and the same ten exposed scenes with stricter collision monitoring. Wider-position development was 1/3. This is not full reachable-workspace coverage or continuous visual correction.
- The original three-view model remains for familiar sideways practice. Its earlier broader ten-scene test was 0/10; keep that failure evidence.
- Learned actions never use exact simulator object positions, a programmed teacher, action retrieval, attachments or teleportation. A separate privileged-state monitor can stop/score execution. Learned success verifies release before the working arm necessarily finishes parking; reset before another learned trial.
- Other dishes and arbitrary destinations are not learned skills. Unsupported learned instructions must be refused explicitly, without a silent programmed fallback.
- The laptop passed 40 application tests and 9 training-contract tests from a clean source checkout. Dependencies were reused there, so setup on this PC is a new installation check.

## Persistent instructions

**September 14 observer adaptation:** the separately declared [paired-camera fit](robotics/evidence/rgb-servo-observer-camera-v1/README.md) completes all 6,000 updates in 207.78 seconds / 2.912 GiB peak Torch-reserved memory. Coverage-first development selection chooses step 3,000; FP32 export and CPU development checks pass. Fresh perception accepts 226/232 present bottles and no absent scenes out of 24, with 2.293 mm p95, but fails the unchanged 6 mm maximum limit at 7.124 mm. It stops before physics; all reserved physical scenes remain unexposed. Both checkpoints, compact recipes/labels, actual IR and all observations are packaged and independently audited. Continue from the final review packet, prioritizing actual Intel execution, the pending human full-sequence voice rehearsal and release access. No additional fitting belongs to this stopped protocol. Keep GitHub/HF private and never click Submit.

**September 14 continuation:** the [fixed-camera diagnostic](robotics/evidence/rgb-servo-camera-visibility-v1/README.md) selects opposite oblique views on exposed saved states: 122/128 accepted, all 11 original refusals recovered, 2.524 mm p95. The subsequent [fresh perception test](robotics/evidence/rgb-servo-camera-perception-v1/README.md) completes all 512 paired observations but fails acceptance: 202/237 present bottles, below 90%, despite accurate accepted estimates and 0/19 absent false accepts. It stops before physical trials; all evidence is packaged. Future camera work needs a new compact observer-training protocol and fresh splits, not further tuning within this completed test. The private hosted seven-step workflow also passes after the 420-second allowance update, in 305.33 wall seconds, exceeding the old limit. Preserve the exact [post-update browser evidence](robotics/evidence/composed-dinner-v1/cloud-cpu-headroom-seed42.json).

Check **both the output directory and console-log filename for prior existence before launching any redirected job**. One September 14 launch replaced an older local console log before the diagnostic correctly refused its existing output directory. The original JSON results and 27 PNGs remain intact; the old console text was not recovered. The incident is recorded locally in `.run/final-goal/visibility-console-log-collision-20260914.json`. All 41 protected published artifacts still match their preserved hashes. Do not reuse that old log as evidence of its original run.

**Public-release timing (September 13 correction):** keep the GitHub repository private until immediately before the final submission. The user explicitly reversed the early public visibility change. The repository is private again, anonymous access returns 404, and GitHub Pages plus its publishing workflow are disabled. Push sanitized development commits privately; do not make the repository or a duplicate source repository public early. Prepare and test hosting privately/local first.

Check the destination drive before **every** new training batch/episode or export and periodically during writing. Preserve at least **10 GiB free in addition to expected writes**. Never automatically delete existing data or checkpoints. Use compact state data and separate demo rendering where possible.

The user authorizes pushing needed, sanitized project changes to the existing private GitHub repository. Public visibility follows the timing rule above. Use a `codex/` branch for new work and a nonpersonal commit identity such as `Talos contributors <contributors@talos.invalid>`. Inspect staged content for credentials/private paths; do not push old laptop branches or backups containing the previous personal commit identity. All current remote history was sanitized.

The user will upload the hackathon submission. Materials are in `submission/`; no public hosted interactive simulator has been deployed. Keep claims aligned with measured results and distinguish programmed skills, classical vision and neural control.
